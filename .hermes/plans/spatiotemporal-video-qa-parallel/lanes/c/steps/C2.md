# Step C2 — Evidence-linked aggregate report

## Scope
Join public question, separately loaded oracle, status, family/predicate, evidence, and source hash; add diagnostic controls.

Owned files: `scoring.py, runner.py, test_scoring.py, test_runner.py` plus this lane ledger.

## Checklist
- [ ] Write one failing behavioral test.
- [ ] Run it and observe the expected missing-behavior failure.
- [ ] Implement the minimum behavior.
- [ ] Run the focused gate and static checks.
- [ ] Resolve one focused independent review.
- [ ] Run both ownership guards, update progress, commit explicit files, push, and verify remote SHA.

## Focused gate
`uv run pytest dimos/benchmark/spatiotemporal/test_scoring.py dimos/benchmark/spatiotemporal/test_runner.py -v`

## Commit
`feat(benchmark): report evidence-linked candidate scores`
