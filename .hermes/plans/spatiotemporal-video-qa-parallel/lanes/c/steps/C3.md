# Step C3 — TemporalMemory candidate adapter seam

## Scope
Implement frozen CandidateAnswerer; fake tests prove ingestion before questions and no teacher artifacts; cleanup is mandatory.

Owned files: `temporal_memory_answerer.py, test_temporal_memory_answerer.py` plus this lane ledger.

## Checklist
- [ ] Write one failing behavioral test.
- [ ] Run it and observe the expected missing-behavior failure.
- [ ] Implement the minimum behavior.
- [ ] Run the focused gate and static checks.
- [ ] Resolve one focused independent review.
- [ ] Run both ownership guards, update progress, commit explicit files, push, and verify remote SHA.

## Focused gate
`uv run pytest dimos/benchmark/spatiotemporal/test_temporal_memory_answerer.py -v`

## Commit
`feat(benchmark): add a TemporalMemory candidate adapter`
