# Step D2 — Replay API and entry point

## Scope
Consume saved observations via frozen generation/bundle APIs; actionable insufficiency diagnostics; identical logical hashes across roots.

Owned files: `replay.py, test_replay.py` plus this lane ledger.

## Checklist
- [ ] Write one failing behavioral test.
- [ ] Run it and observe the expected missing-behavior failure.
- [ ] Implement the minimum behavior.
- [ ] Run the focused gate and static checks.
- [ ] Resolve one focused independent review.
- [ ] Run both ownership guards, update progress, commit explicit files, push, and verify remote SHA.

## Focused gate
`uv run pytest dimos/benchmark/spatiotemporal/test_replay.py -v`

## Commit
`feat(benchmark): replay observations into evaluation bundles`
