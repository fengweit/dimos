# Step D1 — Canonical observation JSONL

## Scope
Canonical byte-stable round trip; reject duplicate/conflicting IDs, NaN, malformed boxes/timestamps, and non-NFC strings.

Owned files: `observation_io.py, test_observation_io.py` plus this lane ledger.

## Checklist
- [ ] Write one failing behavioral test.
- [ ] Run it and observe the expected missing-behavior failure.
- [ ] Implement the minimum behavior.
- [ ] Run the focused gate and static checks.
- [ ] Resolve one focused independent review.
- [ ] Run both ownership guards, update progress, commit explicit files, push, and verify remote SHA.

## Focused gate
`uv run pytest dimos/benchmark/spatiotemporal/test_observation_io.py -v`

## Commit
`feat(benchmark): persist canonical teacher observations`
