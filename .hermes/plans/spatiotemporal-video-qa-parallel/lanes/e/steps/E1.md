# Step E1 — OpenCV sampler and fake detector seam

## Scope
Deterministic sampling/normalization, fake detector observations, explicit decode/stride/dimension failures, and cleanup; no heavy assets.

Owned files: `video_adapter.py, test_video_adapter.py` plus this lane ledger.

## Checklist
- [ ] Write one failing behavioral test.
- [ ] Run it and observe the expected missing-behavior failure.
- [ ] Implement the minimum behavior.
- [ ] Run the focused gate and static checks.
- [ ] Resolve one focused independent review.
- [ ] Run both ownership guards, update progress, commit explicit files, push, and verify remote SHA.

## Focused gate
`uv run pytest dimos/benchmark/spatiotemporal/test_video_adapter.py -v`

## Commit
`feat(benchmark): sample video into detector frames`
