# Step E2 — YOLO-E identity adapter

## Scope
Preserve native track IDs; constrained class fallback only for -1 and unique labels; lazy imports, cleanup, continuity/drop statistics.

Owned files: `yoloe_adapter.py, test_yoloe_adapter.py` plus this lane ledger.

## Checklist
- [ ] Write one failing behavioral test.
- [ ] Run it and observe the expected missing-behavior failure.
- [ ] Implement the minimum behavior.
- [ ] Run the focused gate and static checks.
- [ ] Resolve one focused independent review.
- [ ] Run both ownership guards, update progress, commit explicit files, push, and verify remote SHA.

## Focused gate
`uv run pytest dimos/benchmark/spatiotemporal/test_yoloe_adapter.py -v`

## Commit
`feat(benchmark): adapt YOLO-E video detections`
