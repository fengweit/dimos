# Step A2 — Relation interval construction

## Scope
Coalesce consecutive accepted samples; split on gaps or identity/relation changes; reject conflicting schedules.

Owned files: `intervals.py, test_intervals.py` plus this lane ledger.

## Checklist
- [ ] Write one failing behavioral test.
- [ ] Run it and observe the expected missing-behavior failure.
- [ ] Implement the minimum behavior.
- [ ] Run the focused gate and static checks.
- [ ] Resolve one focused independent review.
- [ ] Run both ownership guards, update progress, commit explicit files, push, and verify remote SHA.

## Focused gate
`uv run pytest dimos/benchmark/spatiotemporal/test_intervals.py -v`

## Commit
`feat(benchmark): build replayable relation intervals`
