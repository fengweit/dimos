# Step 02b — Ambiguity and metamorphic harness

## Scope

Harden relation uncertainty and invariants.

## Checklist

- [ ] RED: overlap, equality-at-margin, missing evidence, invalid margins.
- [ ] Add deterministic seeded mirror/translation/input-order checks.
- [ ] GREEN/refactor without changing accepted semantics.
- [ ] Run Phase 2 H2 harness and adversarial review.
- [ ] Update progress and push.

## Focused harness

```bash
uv run pytest dimos/benchmark/spatiotemporal/test_relations.py -v
```

## Done only when

The focused behavior is replayable, required checks are green in the main worktree, review findings are resolved or explicitly rejected with rationale, `progress.md` is updated, and the commit is present on `fork/feat/spatiotemporal-video-qa`.

## Commit

```text
test(benchmark): harden spatial relation invariants
```
