# Step 09b — Real candidate smoke and evidence report

## Scope

Run actual TemporalMemory on the same video.

## Checklist

- [ ] Verify required credentials/assets or stop with one blocker.
- [ ] Produce real predictions for public questions.
- [ ] Score with separately loaded oracle.
- [ ] Inspect one correct and one non-correct trace; label diagnostic mutation if needed.
- [ ] Update progress and push.

## Focused harness

```bash
uv run pytest dimos/benchmark/spatiotemporal -v
```

## Done only when

The focused behavior is replayable, required checks are green in the main worktree, review findings are resolved or explicitly rejected with rationale, `progress.md` is updated, and the commit is present on `fork/feat/spatiotemporal-video-qa`.

## Commit

```text
test(benchmark): verify a real TemporalMemory candidate
```
