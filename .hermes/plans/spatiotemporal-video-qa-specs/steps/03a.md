# Step 03a — Relation intervals and gaps

## Scope

Create interval derivation from ordered sample schedules.

## Checklist

- [ ] RED: consecutive accepted relations coalesce.
- [ ] Missing/empty samples and relation changes split intervals.
- [ ] Reject conflicting frame/timestamp identities.
- [ ] GREEN: implement minimum interval builder.
- [ ] Update progress and push.

## Focused harness

```bash
uv run pytest dimos/benchmark/spatiotemporal/test_intervals.py -v
```

## Done only when

The focused behavior is replayable, required checks are green in the main worktree, review findings are resolved or explicitly rejected with rationale, `progress.md` is updated, and the commit is present on `fork/feat/spatiotemporal-video-qa`.

## Commit

```text
feat(benchmark): build replayable relation intervals
```
