# Step 01c — One public question and private answer

## Scope

Create `questions.py` and `test_questions.py` for one accepted relation.

## Checklist

- [ ] RED: one relation yields one stable public question and separate private answer.
- [ ] Prove public serialization has no answer/evidence fields.
- [ ] GREEN: implement only the spatial question builder.
- [ ] Update progress and push.

## Focused harness

```bash
uv run pytest dimos/benchmark/spatiotemporal/test_questions.py -v
```

## Done only when

The focused behavior is replayable, required checks are green in the main worktree, review findings are resolved or explicitly rejected with rationale, `progress.md` is updated, and the commit is present on `fork/feat/spatiotemporal-video-qa`.

## Commit

```text
feat(benchmark): build one evidence-linked spatial question
```
