# Step 05a — Bundle writer and loader

## Scope

Create deterministic public/private bundle IO.

## Checklist

- [ ] RED: write and strict round-trip load.
- [ ] Public contains episode metadata/questions only.
- [ ] Oracle contains teacher observations/intervals/answers.
- [ ] Foreign and duplicate references fail.
- [ ] Update progress and push.

## Focused harness

```bash
uv run pytest dimos/benchmark/spatiotemporal/test_bundles.py -k "write or load or round" -v
```

## Done only when

The focused behavior is replayable, required checks are green in the main worktree, review findings are resolved or explicitly rejected with rationale, `progress.md` is updated, and the commit is present on `origin/feat/spatiotemporal-video-qa`.

## Commit

```text
feat(benchmark): write replayable evaluation bundles
```
