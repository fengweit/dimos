# Step 05b — Hashes, leakage, and path safety

## Scope

Harden bundle integrity.

## Checklist

- [ ] RED: corruption, traversal, absolute paths, and hash mismatch fail.
- [ ] Public leakage scan rejects teacher boxes/answers/evidence.
- [ ] Separate roots produce identical canonical hashes.
- [ ] Run Phase 5 H3/H4 and security review.
- [ ] Update progress and push.

## Focused harness

```bash
uv run pytest dimos/benchmark/spatiotemporal/test_bundles.py -v
```

## Done only when

The focused behavior is replayable, required checks are green in the main worktree, review findings are resolved or explicitly rejected with rationale, `progress.md` is updated, and the commit is present on `origin/feat/spatiotemporal-video-qa`.

## Commit

```text
test(benchmark): enforce bundle integrity and isolation
```
