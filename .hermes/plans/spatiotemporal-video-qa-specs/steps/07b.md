# Step 07b — Observation replay entry point

## Scope

Replay saved observations through the hermetic generator.

## Checklist

- [ ] RED: saved observations regenerate the same bundle.
- [ ] Add actionable insufficiency diagnostics.
- [ ] Run twice in separate roots and compare bytes/hashes.
- [ ] Run Phase 7 H3/H4 and reviews.
- [ ] Update progress and push.

## Focused harness

```bash
uv run pytest dimos/benchmark/spatiotemporal/test_replay.py -v
```

## Done only when

The focused behavior is replayable, required checks are green in the main worktree, review findings are resolved or explicitly rejected with rationale, `progress.md` is updated, and the commit is present on `origin/feat/spatiotemporal-video-qa`.

## Commit

```text
feat(benchmark): replay observations into evaluation bundles
```
