# Step 02a — Inverse and vertical predicates

## Scope

Extend the pure relation oracle without changing contracts.

## Checklist

- [ ] RED: right-of, above, and below examples and inverse consistency.
- [ ] GREEN: implement by argument/axis reuse, not copy-pasted policy.
- [ ] Verify all four predicates.
- [ ] Update progress and push.

## Focused harness

```bash
uv run pytest dimos/benchmark/spatiotemporal/test_relations.py -v
```

## Done only when

The focused behavior is replayable, required checks are green in the main worktree, review findings are resolved or explicitly rejected with rationale, `progress.md` is updated, and the commit is present on `origin/feat/spatiotemporal-video-qa`.

## Commit

```text
feat(benchmark): add inverse and vertical relations
```
