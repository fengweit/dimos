# Step 03b — Strict before and after

## Scope

Add tri-state temporal ordering over intervals.

## Checklist

- [ ] RED: strict non-overlap proves before/after.
- [ ] Touching, overlap, containment, and missing IDs are unknown.
- [ ] Add time-shift and input-order invariants.
- [ ] Run Phase 3 boundary reviews.
- [ ] Update progress and push.

## Focused harness

```bash
uv run pytest dimos/benchmark/spatiotemporal/test_intervals.py -v
```

## Done only when

The focused behavior is replayable, required checks are green in the main worktree, review findings are resolved or explicitly rejected with rationale, `progress.md` is updated, and the commit is present on `fork/feat/spatiotemporal-video-qa`.

## Commit

```text
feat(benchmark): derive strict temporal ordering
```
