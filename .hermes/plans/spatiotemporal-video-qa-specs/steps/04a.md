# Step 04a — Spatial-at question generation

## Scope

Generate deterministic spatial questions from accepted evidence.

## Checklist

- [ ] RED: accepted facts generate stable spatial-at questions.
- [ ] Unknown candidates never generate Boolean questions.
- [ ] Preserve evidence/provenance privately.
- [ ] Update progress and push.

## Focused harness

```bash
uv run pytest dimos/benchmark/spatiotemporal/test_generation.py -k spatial -v
```

## Done only when

The focused behavior is replayable, required checks are green in the main worktree, review findings are resolved or explicitly rejected with rationale, `progress.md` is updated, and the commit is present on `origin/feat/spatiotemporal-video-qa`.

## Commit

```text
feat(benchmark): generate spatial-at questions
```
