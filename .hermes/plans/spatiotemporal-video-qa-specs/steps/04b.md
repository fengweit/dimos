# Step 04b — Temporal questions and balance

## Scope

Generate before/after questions and a deterministic candidate report.

## Checklist

- [ ] RED: accepted temporal pairs generate positive/inverse variants.
- [ ] Balance families/polarities without inventing labels.
- [ ] Reordering input yields byte-identical logical output.
- [ ] Run Phase 4 H2 and reviews.
- [ ] Update progress and push.

## Focused harness

```bash
uv run pytest dimos/benchmark/spatiotemporal/test_generation.py -v
```

## Done only when

The focused behavior is replayable, required checks are green in the main worktree, review findings are resolved or explicitly rejected with rationale, `progress.md` is updated, and the commit is present on `origin/feat/spatiotemporal-video-qa`.

## Commit

```text
feat(benchmark): generate balanced temporal questions
```
