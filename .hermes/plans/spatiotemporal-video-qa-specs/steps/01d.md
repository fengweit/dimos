# Step 01d — Exact score and tracer

## Scope

Create `scoring.py`, `test_scoring.py`, and `test_tracer.py`.

## Checklist

- [ ] RED: typed matching answer scores correctly; mismatched IDs fail.
- [ ] GREEN: implement one exact scorer.
- [ ] Prove observation → relation → question → private answer → score.
- [ ] Run all Phase 1 tests plus H1/H2 checks.
- [ ] Obtain phase-boundary spec and adversarial reviews.
- [ ] Update progress and push.

## Focused harness

```bash
uv run pytest dimos/benchmark/spatiotemporal -v
```

## Done only when

The focused behavior is replayable, required checks are green in the main worktree, review findings are resolved or explicitly rejected with rationale, `progress.md` is updated, and the commit is present on `origin/feat/spatiotemporal-video-qa`.

## Commit

```text
feat(benchmark): complete the spatial QA tracer bullet
```
