# Step 06a — Prediction parsing and statuses

## Scope

Add strict candidate answer normalization.

## Checklist

- [ ] RED: bool and explicit yes/no parse; other text is invalid.
- [ ] Missing, invalid, incorrect, and correct remain distinct.
- [ ] Stable prediction serialization.
- [ ] Update progress and push.

## Focused harness

```bash
uv run pytest dimos/benchmark/spatiotemporal/test_runner.py -k "parse or status" -v
```

## Done only when

The focused behavior is replayable, required checks are green in the main worktree, review findings are resolved or explicitly rejected with rationale, `progress.md` is updated, and the commit is present on `fork/feat/spatiotemporal-video-qa`.

## Commit

```text
feat(benchmark): parse typed candidate predictions
```
