# Step 06b — Evidence-linked report and candidate protocol

## Scope

Add aggregation and source-video candidate context.

## Checklist

- [ ] RED: per-question result links predicate, expected/predicted/status, evidence, and source hash.
- [ ] Candidate protocol ingests public source-video context before questions.
- [ ] Prove candidate cannot receive teacher records.
- [ ] Add always-yes/final-frame/oracle diagnostic controls.
- [ ] Run Phase 6 reviews.
- [ ] Update progress and push.

## Focused harness

```bash
uv run pytest dimos/benchmark/spatiotemporal/test_runner.py dimos/benchmark/spatiotemporal/test_scoring.py -v
```

## Done only when

The focused behavior is replayable, required checks are green in the main worktree, review findings are resolved or explicitly rejected with rationale, `progress.md` is updated, and the commit is present on `fork/feat/spatiotemporal-video-qa`.

## Commit

```text
feat(benchmark): report evidence-linked candidate scores
```
