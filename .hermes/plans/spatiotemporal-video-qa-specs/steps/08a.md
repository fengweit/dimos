# Step 08a — Video frame sampler and fake detector seam

## Scope

Create deterministic OpenCV sampling behind injected detector protocol.

## Checklist

- [ ] RED: synthetic video/fake detector yields canonical timestamps and boxes.
- [ ] Validate decode failure, stride, dimensions, and normalized coordinates.
- [ ] No model/network in default tests.
- [ ] Update progress and push.

## Focused harness

```bash
uv run pytest dimos/benchmark/spatiotemporal/test_video_adapter.py -k "sample or fake" -v
```

## Done only when

The focused behavior is replayable, required checks are green in the main worktree, review findings are resolved or explicitly rejected with rationale, `progress.md` is updated, and the commit is present on `fork/feat/spatiotemporal-video-qa`.

## Commit

```text
feat(benchmark): sample video into detector frames
```
