# Step 08c — Real video to eval bundle

## Scope

Execute mandatory raw-video generation.

## Checklist

- [ ] Materialize or record a constrained 15–30 second demo video via LFS policy.
- [ ] Generate canonical observations and bundle.
- [ ] Require three identities, one transition, both question families, and at least 12 questions.
- [ ] Replay observations and compare hashes.
- [ ] Run H6 teacher path and Phase 8 reviews.
- [ ] Update progress and push.

## Focused harness

```bash
uv run pytest dimos/benchmark/spatiotemporal -v
```

## Done only when

The focused behavior is replayable, required checks are green in the main worktree, review findings are resolved or explicitly rejected with rationale, `progress.md` is updated, and the commit is present on `origin/feat/spatiotemporal-video-qa`.

## Commit

```text
feat(benchmark): generate an evaluation bundle from video
```
