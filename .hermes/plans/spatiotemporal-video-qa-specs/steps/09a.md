# Step 09a — TemporalMemory candidate adapter

## Scope

Wrap existing TemporalMemory ingestion/query behind candidate protocol.

## Checklist

- [ ] RED with fake stream/query callable.
- [ ] Ingest source video before questions and verify readiness.
- [ ] Never pass teacher observations, boxes, intervals, evidence, or answers.
- [ ] Persist normalized predictions.
- [ ] Update progress and push.

## Focused harness

```bash
uv run pytest dimos/benchmark/spatiotemporal/test_temporal_memory_answerer.py -v
```

## Done only when

The focused behavior is replayable, required checks are green in the main worktree, review findings are resolved or explicitly rejected with rationale, `progress.md` is updated, and the commit is present on `fork/feat/spatiotemporal-video-qa`.

## Commit

```text
feat(benchmark): add a TemporalMemory candidate adapter
```
