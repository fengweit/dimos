# Step 01b — One left-of relation

## Scope

Create `relations.py` and `test_relations.py` with only one relation behavior.

## Checklist

- [ ] RED: separated boxes derive `left-of`; inverse, overlap, same-object, cross-sample, malformed margin, and boundary equality do not invent truth.
- [ ] GREEN: implement the minimum pure oracle.
- [ ] Run focused tests and static checks.
- [ ] Update progress and push.

## Focused harness

```bash
uv run pytest dimos/benchmark/spatiotemporal/test_relations.py -v
```

## Done only when

The focused behavior is replayable, required checks are green in the main worktree, review findings are resolved or explicitly rejected with rationale, `progress.md` is updated, and the commit is present on `origin/feat/spatiotemporal-video-qa`.

## Commit

```text
feat(benchmark): derive one left-of relation
```
