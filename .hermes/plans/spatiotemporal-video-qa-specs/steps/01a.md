# Step 01a — Strict contracts and canonical IDs

## Scope

Create `models.py`, `utilities.py`, `test_models.py`, and `test_utilities.py`.

## Checklist

- [ ] RED: strict/frozen records reject unknown, mutable, malformed, non-NFC, and unstable-ID inputs.
- [ ] GREEN: implement only the minimum contracts and canonical hashing.
- [ ] Verify known relation/question SHA-256 vectors.
- [ ] Run focused tests, Ruff, and targeted mypy.
- [ ] Update progress and push.

## Focused harness

```bash
uv run pytest dimos/benchmark/spatiotemporal/test_models.py dimos/benchmark/spatiotemporal/test_utilities.py -v
```

## Done only when

The focused behavior is replayable, required checks are green in the main worktree, review findings are resolved or explicitly rejected with rationale, `progress.md` is updated, and the commit is present on `origin/feat/spatiotemporal-video-qa`.

## Commit

```text
feat(benchmark): add strict spatiotemporal QA contracts
```
