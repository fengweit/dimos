# Step 08b — YOLO-E adapter and identity contract

## Scope

Map repository detections to canonical observations.

## Checklist

- [ ] RED with fake repository detection objects.
- [ ] Prefer native track IDs; permit one-instance-per-class fallback only.
- [ ] Duplicate fallback labels fail; record continuity/drop stats.
- [ ] Verify no model loads at import.
- [ ] Update progress and push.

## Focused harness

```bash
uv run pytest dimos/benchmark/spatiotemporal/test_video_adapter.py -v
```

## Done only when

The focused behavior is replayable, required checks are green in the main worktree, review findings are resolved or explicitly rejected with rationale, `progress.md` is updated, and the commit is present on `origin/feat/spatiotemporal-video-qa`.

## Commit

```text
feat(benchmark): adapt YOLO-E video detections
```
