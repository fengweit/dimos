# Step 09c — One-command demo and final package

## Scope

Document, replay, review, and package.

## Checklist

- [ ] One command emits observations, bundle, predictions, and report.
- [ ] Run every documented command from clean roots.
- [ ] Run H6/H7, full diff review, secret/binary scan, and clean-shell docs review.
- [ ] Set automation COMPLETE only after real outputs and remote verification.
- [ ] Update progress, push, and verify remote HEAD.

## Focused harness

```bash
uv run pytest --numprocesses=3 -m "not (self_hosted or mujoco or self_hosted_large)"
```

## Done only when

The focused behavior is replayable, required checks are green in the main worktree, review findings are resolved or explicitly rejected with rationale, `progress.md` is updated, and the commit is present on `origin/feat/spatiotemporal-video-qa`.

## Commit

```text
docs(benchmark): add the spatiotemporal video QA demo
```
