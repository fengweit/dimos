# Step 07a — Canonical teacher-observation JSONL

## Scope

Create strict observation writer/reader.

## Checklist

- [ ] RED: canonical order, strict round trip, stable bytes.
- [ ] Reject duplicate/conflicting IDs, NaN, malformed boxes, and bad timestamps.
- [ ] Keep this artifact teacher-private.
- [ ] Update progress and push.

## Focused harness

```bash
uv run pytest dimos/benchmark/spatiotemporal/test_observation_io.py -v
```

## Done only when

The focused behavior is replayable, required checks are green in the main worktree, review findings are resolved or explicitly rejected with rationale, `progress.md` is updated, and the commit is present on `origin/feat/spatiotemporal-video-qa`.

## Commit

```text
feat(benchmark): persist canonical teacher observations
```
